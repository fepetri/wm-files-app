'use strict'

import React from 'react'

const Search = () => (
    <form className="searchFiles">
        <input
            type="search"
            placeholder="Digite o nome do arquivo..."
        />
    </form>
)

export default Search